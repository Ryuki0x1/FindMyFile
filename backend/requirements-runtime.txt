# FindMyPic - Runtime Dependencies (CPU-only, ~500MB total)
# This is for distribution - uses CPU-only PyTorch to reduce size

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# AI/ML - CPU-only versions (smaller size)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu
torchvision==0.17.0+cpu

# AI Models
transformers==4.37.2
sentence-transformers==2.3.1
facenet-pytorch==2.5.3
easyocr==1.7.0

# Vector Database
chromadb==0.4.22

# Image Processing
Pillow==10.2.0
opencv-python-headless==4.9.0.80
exifread==3.0.0

# Utilities
numpy==1.26.3
scipy==1.12.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Note: Total size ~500MB vs 8.6GB for full CUDA build
# Models (CLIP, FaceNet, OCR) will be downloaded on first run (~750MB)
